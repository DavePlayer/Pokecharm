import * as React from 'react'

export const VisitHomePage:React.FC = () => (
    <main>I'VE ENOUGH OF THIS! for 4H I was trying to make file-loader to work just to find out that you had only to add "esModule: false" line to options in file-loader. I commince a small break from this (probably only for today) because at this rate i will commit seppuku.</main>
)